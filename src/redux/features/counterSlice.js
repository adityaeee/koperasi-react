import { createSlice } from "@reduxjs/toolkit";

export const counterSlice = createSlice({
    name: "counter",
    initialState: {
        value: 0,
    },
    reducers: {
        increment: (state) => {
            state.value += 1;
        },
        decrement: (state) => {
            state.value -= 1;
        },
    },
});

// ini untuk dispatecher
export const { increment, decrement } = counterSlice.actions;

// ini untuk si store
export default counterSlice.reducer;
